# Dependencies
node_modules/

# Build output (published to the gh-pages branch by `npm run deploy`, not tracked here)
dist/
dist.zip
*.tsbuildinfo

# Test & tooling output
playwright-report/
test-results/
graphify-out/
.vite/

# Local environment (keep .env.example tracked as the template)
.env
.env.local
.env.production
*.local

# OS / editor junk
.DS_Store
Thumbs.db

# Internal planning docs (not for the repo)
docs/
_internal/

# Vite dev server artifacts
vite.config.js.timestamp-*

# Local Netlify folder
.netlify

# Local Claude Code skills (not shared with the team)
.claude/skills/impeccable/
