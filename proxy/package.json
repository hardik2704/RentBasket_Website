{
  "name": "rentbasket-api-proxy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Edge proxy that injects RentBasket API secret keys server-side (Option A).",
  "scripts": {
    "dev": "node dev-server.js"
  }
}
